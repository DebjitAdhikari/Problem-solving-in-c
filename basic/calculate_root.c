#include <stdio.h>
#include <math.h>
int main()
{
    int num,sq;
    printf("Enter the num\n");
    scanf("%d",&num);
    sq=sqrt(num);
    printf("The square root of the num is %d",sq);
    return 0;
}
